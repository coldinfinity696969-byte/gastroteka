Векторные исходники слоёв круга из истории (печать, чашка, блюда).
На сайте используются готовые картинки из `img/orb/`: они отрисованы из этих SVG
в 1200 px (печать ещё и в 1800 px) и сохранены в WebP с прозрачностью.
`face-front.svg` ссылается на `img/logo-art.svg` и символ `#wm` из `index.html`.
